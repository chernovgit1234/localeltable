import { IConditionFormattingRules } from "@/types/FormattingTypes";

export interface ConfigData {
  rulesMap: Map<number, IConditionFormattingRules[]>;
}

/* export interface IForma {
  id: string,
  name: string,
  settings?: {}
} */